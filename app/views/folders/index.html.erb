<h1>Folders</h1>
<% if @folders.count > 0  %>
<ul>
  <% @folders.each do |folder| %>
    <li><%= link_to folder.name, folder_show_path(path: folder.path) %><br />
        <%= link_to "Copy", copy_start_path(folder.path) %><br /><br />
    </li>
  <% end %>
</ul>
<% end %>
